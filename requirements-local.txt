# Torch 2.6.0+cu124 / torchvision 0.21.0+cu124 are installed separately by setup.ps1.
# Official TRIBE source is installed from vendor/tribev2 at pinned commit.
transformers>=4.53,<5
mne>=1.8,<2
scipy>=1.14,<2
matplotlib>=3.9,<4
imageio-ffmpeg>=0.6,<1
uv>=0.4,<1
# Used directly by this project (previously only present via manual installs / transitive deps)
Flask>=3.1,<4
nibabel>=5,<6
requests>=2.31,<3
